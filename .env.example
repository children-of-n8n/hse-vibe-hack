POSTGRES_USER=postgres
# WARNING: Replace the password below with a strong, unique value before deployment!
POSTGRES_PASSWORD=REPLACE_WITH_SECURE_PASSWORD
POSTGRES_DB=postgres
# WARNING: Replace the JWT secret below with a strong, unique value before deployment!
JWT_SECRET=REPLACE_WITH_SECURE_PASSWORD
PORT=3000
AUTO_MIGRATE=true
AUTO_GENERATE_MIGRATIONS=true
