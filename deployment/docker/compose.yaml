services:
  backend:
    image: ghcr.io/children-of-n8n/hse-vibe-hack-backend:latest
    environment:
      - NODE_ENV=production
      - PORT=3000
    networks:
      - application

networks:
  application:
    driver: bridge
